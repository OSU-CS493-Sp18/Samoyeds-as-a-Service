const router = require('express').Router();

//Pops and sends oldest report to review
router.get('/', (req, res) =>{

});

//Adds report to Redis queue
router.put('/', (req, res) =>{

});

exports.router = router;
